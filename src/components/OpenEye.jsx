export function OpenEye({ size = 5 }) {
  return (
    <img
      src="/pictures/open_eye.png" alt= "" className={`w-${size} h-${size} `}
    />
  );
}

export function ClosedEye({ size = 5 }) {
  return (
    <img
      src="/pictures/closed_eye.png" alt= "" className={`w-${size} h-${size} `}
    />
  );
}